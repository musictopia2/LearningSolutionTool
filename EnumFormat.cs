namespace LearningSolutionTool;
public enum EnumFormat
{
    Main,
    Analyzer,
    SourceGenerator,
    CodeFix //figure out as i go along what else is needed

    //can't do code fixes until i did more with analyzers since code fixes are more complex.
}
